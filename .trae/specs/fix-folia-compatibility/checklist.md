- [x] `FoliaCompat` 工具类已创建，包含 Folia 环境检测逻辑
- [x] `FoliaCompat.runTaskTimerAsync()` 在 Folia 环境下使用 `AsyncScheduler`，非 Folia 下使用传统调度器
- [x] `FoliaCompat.runTaskGlobal()` 在 Folia 环境下使用 `GlobalRegionScheduler`，非 Folia 下使用传统调度器
- [x] `VerifyMC.java` 中 AuthMe 周期同步使用 `FoliaCompat.runTaskTimerAsync()`，不再抛出 `UnsupportedOperationException`
- [x] `VerifyMC.onDisable()` 中正确取消 AuthMe 周期同步任务
- [x] `DiscordService.java` 中 Token 清理使用 `FoliaCompat.runTaskTimerAsync()`，不再使用 `BukkitRunnable`
- [x] `DiscordService` 提供 `stop()` 方法用于取消定时任务
- [x] `AuthmeService.java` 中白名单命令使用 `FoliaCompat.runTaskGlobal()`
- [x] `RegistrationProcessingHandler.java` 中白名单命令使用 `FoliaCompat.runTaskGlobal()`
- [x] `AdminUserDeleteHandler.java` 中白名单命令使用 `FoliaCompat.runTaskGlobal()`
- [x] `AdminUserUnbanHandler.java` 中白名单命令使用 `FoliaCompat.runTaskGlobal()`
- [x] `AdminUserApproveHandler.java` 中白名单命令使用 `FoliaCompat.runTaskGlobal()`
- [x] `AdminUserBanHandler.java` 中白名单命令使用 `FoliaCompat.runTaskGlobal()`
- [x] 项目编译通过，无编译错误
